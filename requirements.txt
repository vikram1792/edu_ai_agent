# ==== Core AI Framework ====
langgraph==0.0.40                # LangGraph for building the agent flow
langchain==0.1.20                # LangChain core functionality
langchain-core==0.1.50           # Core abstractions used by LangGraph and LangChain
langchain-openai==0.1.8          # OpenAI integration for LangChain/Graph
openai==1.30.1                   # OpenAI Python SDK

# ==== Web Scraping ====
beautifulsoup4==4.12.3           # HTML parser for Wikipedia scraping
requests==2.31.0                 # HTTP library for fetching web content

# ==== PDF Processing ====
pypdf==4.1.0                     # For parsing text-based PDFs via PyPDFLoader
pdf2image==1.17.0                # Convert PDF pages to images for OCR fallback

# ==== OCR ====
pytesseract==0.3.10              # Tesseract OCR for image-based text extraction

# ==== Utility Dependencies ====
Pillow==10.3.0                   # Required by pdf2image for image handling
tqdm==4.66.2                     # Optional, for progress bars if needed

# ==== Deployment (Streamlit etc., if you're adding later) ====
# streamlit==1.33.0             # Optional: Include when building UI
